name: Linting
on: pull_request
jobs:
  biome:
    name: Biome
    runs-on: Ubuntu
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.10
      - run: bun ci
      - run: bun biome:check
