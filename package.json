{
  "name": "prato-feito-frontend",
  "description": "Interface web do PratoFeito, uma comunidade para compartilhar receitas testadas, achadinhos gastronômicos, dicas de cozinha e experiências reais com comida.",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "build": "bun build ./src/index.html --outdir=dist --sourcemap --target=browser --minify --define:process.env.NODE_ENV='\"production\"' --env='BUN_PUBLIC_*'",
    "start": "NODE_ENV=production bun src/index.ts",
    "check": "bunx biome check ./src ./tests",
    "check:fix": "bunx biome check --write ./src ./tests"
  },
  "dependencies": {
    "react": "19",
    "react-dom": "19"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.5",
    "@types/bun": "1.3.10",
    "@types/react": "19",
    "@types/react-dom": "19"
  }
}
